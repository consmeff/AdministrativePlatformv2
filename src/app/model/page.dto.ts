export interface sidebarStateDTO {
  isvisible: boolean;
}

export interface role {
  name: string;
  code: number;
}
export interface appstatus {
  name: string;
  code: number;
}

export interface Column {
  field: string;
  header: string;
}
